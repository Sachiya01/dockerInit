name: CI Pipe

on:
  push:
    branches: ["master"]


jobs: 
  build: 
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repo
        uses: actions/checkout@v3

      - name: Run compose
        run: docker compose up -d

      - name: Run tests
        run: docker ps

